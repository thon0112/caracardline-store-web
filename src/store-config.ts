/**
 * Temporary storefront toggles — edit here for a quick local override.
 *
 * Set `CARD_POOL_ENABLED` to `false` to hide 卡拉福袋 (`card_pool`) from the
 * catalog/search grid and treat direct `/item/:slug` links as not found.
 * Existing cart lines are unchanged.
 */
export const CARD_POOL_ENABLED = true;

export const isCardPoolEnabled = CARD_POOL_ENABLED;
